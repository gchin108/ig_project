import SkeletonCard from "@/components/skeletonCard";
import React from "react";

export default function Loading() {
  return (
    <div className="animate-pulse text-xl flex items-center justify-center h-screen ">
      Loading...
    </div>
  );
}
